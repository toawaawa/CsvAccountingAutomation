Input a csv file(in utf-8) format and it would output a processed csv file
steps left to users:
1. move the amount figure in the rightmost to the normal place
2. change the closing description
3. check if the total sum=0
